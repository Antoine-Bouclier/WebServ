# Webserv: Writing your own HTTP server.

The main goal of this project is to build a HTTP web server in C++98. The server must be able to handle multiple client connections, and perform the following request : **HTTP**, **GET**, **HEAD**, **POST**, **PUT** and **DELETE**.

## Usage

```
make
./webser [File]
```

## Introduction